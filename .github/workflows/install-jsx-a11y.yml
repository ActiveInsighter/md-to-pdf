name: Install JSX accessibility lint dependency

on:
  push:
    branches:
      - 'agent/repository-hygiene-smoke-command'
    paths:
      - '.github/workflows/install-jsx-a11y.yml'
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

jobs:
  install:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Mark dependency installation pending
        uses: actions/github-script@v7
        with:
          script: |
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'pending',
              context: 'jsx-a11y-dependency',
              target_url: runUrl,
              description: 'Installing ESLint 9 accessibility toolchain',
            })

      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install compatible accessibility lint toolchain
        working-directory: frontend
        run: |
          npm install --save-dev \
            '@eslint/js@^9' \
            'eslint@^9' \
            'eslint-plugin-jsx-a11y@6.10.2' \
            --no-audit --no-fund

      - name: Verify clean install and dependency tree
        working-directory: frontend
        run: |
          npm ci --no-audit --no-fund
          npm ls eslint @eslint/js eslint-plugin-jsx-a11y

      - name: Commit package metadata
        run: |
          if git diff --quiet -- frontend/package.json frontend/package-lock.json; then
            echo 'No dependency metadata changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add frontend/package.json frontend/package-lock.json
          git commit -m 'build(frontend): add compatible JSX accessibility lint toolchain'
          git push

      - name: Publish dependency installation result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const state = '${{ job.status }}' === 'success' ? 'success' : 'failure'
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              context: 'jsx-a11y-dependency',
              target_url: runUrl,
              description: state === 'success'
                ? 'Compatible JSX accessibility lint toolchain installed'
                : 'Accessibility lint toolchain installation failed',
            })